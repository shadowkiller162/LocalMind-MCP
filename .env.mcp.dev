# MCP 開發環境配置
# 針對本地 LM Studio 和 DeepSeek R1 模型優化的超時設定

# 基本協議超時（秒）
MCP_CONNECTION_TIMEOUT=60
MCP_REQUEST_TIMEOUT=300

# LM Studio 配置
MCP_LMSTUDIO_HOST=host.docker.internal
MCP_LMSTUDIO_PORT=1234
MCP_LMSTUDIO_TIMEOUT=300

# Ollama 配置
MCP_OLLAMA_HOST=host.docker.internal
MCP_OLLAMA_PORT=11434
MCP_OLLAMA_TIMEOUT=300

# 預設服務和模型
MCP_DEFAULT_LLM_SERVICE=lmstudio
MCP_DEFAULT_MODEL=deepseek-r1-distill-qwen-7b

# 快取配置
MCP_ENABLE_CACHE=true
MCP_CACHE_TTL=7200
MCP_CACHE_MAX_SIZE=2000

# 日誌級別（開發環境使用 DEBUG）
MCP_LOG_LEVEL=DEBUG